parameters:
  message:
    description: "The message to output from this step"
steps:
- name: hello-world
  image: relaysh/core
  input:
  - echo "Hello world. Let's try this again, but again. Your message was $(ni get -p {.message})"
